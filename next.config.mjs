/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['qrypanenvhynvkzvdhjl.supabase.co'], // replace with your Supabase project domain
  },
};

export default nextConfig;
